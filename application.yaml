server:
  port: 8080
spring:
  application:
    name: greeting-service
management:
  endpoints:
    web:
      exposure:
        include: "health, metrics, env, endpoints"
        #- "*"
  endpoint:
    health:
      show-details: always
greeting-service:
  greeting: "Hello"
  defaultValue: "World"